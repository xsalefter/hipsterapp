export class City {
    constructor(
        public id?: number,
        public name?: string,
        public countryCode?: string,
        public district?: string,
        public population?: number,
    ) {
    }
}
